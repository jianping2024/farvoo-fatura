# Farvoo 开票 · UI 交互原型 v2

> **状态：定稿**（**流程、导航、业务用语**；视觉与组件库非最终）  
> **权威：是**（产品 UI 流程与导航以本文 + 静态页为准；工程里程碑见 [`fiscal-dev-plan.zh.md`](../fiscal-dev-plan.zh.md) **M2.6**）  
> **对应实现：** 未接 API；正式 Admin 落地时以本原型为验收参照。

## 设计原则（v2）

1. **先有订单，后有发票** — 手动开票同样：新建订单 → 加商品 → 确认 → 选发票类型 → 签发。
2. **业务语言** — 界面不出现「草稿、LOCAL、API、M3」等开发词；餐馆侧收银同步叫 **「待开票账单」**（内部仍 `bill_sync_drafts`，见 [`fiscal-bill-draft-workbench.zh.md`](../fiscal-bill-draft-workbench.zh.md) §0）。
3. **业态分离** — 登录时选 **餐馆 / 商超**；导航与首页按模式显示不同入口（餐馆才有「待开票账单」）。
4. **流程可见** — 订单/开票页顶栏 **四步进度条**：创建订单 → 添加商品 → 确认订单 → 开票。

## 打开

```bash
open docs/fiscal-admin-ui-prototype/index.html
```

## 导航（P0）

| 菜单 | 餐馆 | 商超 |
|------|------|------|
| 工作台 | ✓ | ✓ |
| 订单 | ✓ | ✓ |
| 待开票账单 | ✓ | — |
| 发票 | ✓ | ✓ |
| 商品 | ✓ | ✓ |
| 客户 | ✓ | ✓ |
| 设置 | ✓ | ✓ |

## 可点路径

| 步骤 | 餐馆 | 商超 |
|------|------|------|
| 登录 | 选餐馆 + PIN | 选商超 + PIN |
| 工作台 | 新建订单 / 待开票账单 | 新建订单 |
| 订单 | 列表 → 继续开票 | 同左 |
| 新建订单 | 桌号 + 商品 + 确认 | 收银台 + 商品 + 确认 |
| 开票 | 选 FT + 购方 + 打印 | 同左 |
| 待开票账单 | 收银同步 → 转订单 | （无此菜单） |
| 发票 | 列表 → 详情 → 重打 | 同左 |

## 与实现的映射（讨论用，不在 UI 展示）

| 原型（业务） | 实现（内部） | 里程碑 |
|--------------|--------------|--------|
| 订单 | 销售快照 / 手动订单聚合 | M2.6 |
| 待开票账单 | `bill_sync_drafts` + bill-sync ingest | M2.5（API）/ M2.6（UI） |
| 签发发票 | `IssueDocument` → FT | M0+ |
| 手动订单开票 | `POST /local/v1/fiscal-documents/manual` | M2.6a ✓ |
| 商品 | `fiscal_products` LOCAL | M2.6a ✓ |
| 客户 | `customers` | M2.6a ✓ |
| 重打 | `POST .../fiscal-documents/{id}/reprints` | M2.6b |
| 冲销 | NC API | M3 |

## 非目标（本目录）

- 不接真实 API
- 不是最终视觉稿 / 组件库选型
- 不定义 NC、SAF-T 详细交互（M3/M5 另文）

## 修订记录

| 日期 | 变更 |
|------|------|
| 2026-08-22 | v2：订单四步、餐馆/商超、业务用语；Will 确认流程对齐 |
| 2026-08-22 | 升为 **定稿**（流程/导航）；挂接 M2.6 开发计划 |
